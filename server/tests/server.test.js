const { graphql } = require('graphql');

const server = require('../server.js');

describe('GraphQL API', () => {
  it.todo(`Returns { "data": { "info": "This is the API for Cody's Untitled Game." } }`);
});